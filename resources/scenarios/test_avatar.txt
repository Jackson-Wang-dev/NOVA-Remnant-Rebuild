@<|
label("test_avatar")
is_unlocked_start()
|>
<|
set_box()
|>
测试头像系统(M6)。注意全程没有调用 show() 显示任何角色立绘——头像系统应该完全独立于立绘是否在场上显示。

<|
avatar("normal")
|>
张浅野::M6 测试:我现在说话,头像应该显示我的 normal pose 头像裁切

<|
avatar("normal")
|>
王二宫::M6 测试:换我说话,而且我也调用了 avatar(),头像应该立刻换成我的裁切(裁切框 x 偏移跟张浅野不一样,应该能看出差别)

孙西本::M6 测试:换孙西本说话,但这句没有调用 avatar()——孙西本之前从没设置过头像,所以头像应该消失

王二宫::M6 测试:再换回王二宫说话,这句也没有调用 avatar()——但王二宫之前设置过的头像应该自动重新出现(粘性效果),不需要每句都重新调用 avatar()

<|
avatar_hide()
|>
王二宫::M6 测试:我隐藏了自己的头像,这句话头像应该消失

王二宫::M6 测试:头像应该保持隐藏,因为上面是显式隐藏,不是切换说话人导致的

<|
avatar("normal")
set_box("left")
|>
张浅野::M6 测试:重新设置头像,同时切换到 left 对话框样式——头像应该在新样式的对话框里同样正确显示(全局状态,不依赖具体哪个对话框实例)

<|
avatar_clear()
set_box("bottom")
|>
M6 测试结束,头像应该已清空,切回 bottom 样式

第一章结束

@<| is_end() |>
