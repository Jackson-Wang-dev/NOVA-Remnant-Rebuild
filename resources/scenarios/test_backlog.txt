@<|
label("test_backlog", "回看测试")
is_unlocked_start()
|>
回看测试开始。先走几句普通对话,再开自动配音,最后用环形菜单的 1 号扇区(文档图标)打开回看面板。

这是第一句没有配音的普通对话,应该能在回看里看到文字,但没有播放语音的按钮

这是第二句没有配音的普通对话

<|
auto_voice_on("张浅野", 1001)
|>
现在给"张浅野"开启自动配音

张浅野::这句话应该自动播放配音 001001,回看里这一条应该带"播放语音"按钮,点击能再听一次

张浅野::这句话应该自动播放配音 001002,回看里这一条也应该带"播放语音"按钮

<|
auto_voice_off("张浅野")
|>

这是配音关闭后的普通对话,回看里不带播放语音按钮

到这里先打开回看面板(环形菜单 1 号扇区,文档图标):应该按顺序看到以上几条记录,有配音的两条带播放按钮,点击能听到对应的配音;点其中一条文字应弹出"要回到此处吗"确认框,确认后应该跳回那一句,且回看列表里这一句之后的记录应该消失(因为已经被回退掉了)

跳回之后再往下走到这里,验证能正常继续推进剧情,回看也能正常追加新记录

测试结束
@<| is_end() |>
